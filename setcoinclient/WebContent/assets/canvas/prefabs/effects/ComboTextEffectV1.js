
// -- user code here --

/* --- start generated code --- */

// Generated by  1.5.2 (Phaser v2.6.2)


/**
 * ComboTextEffectV1
 * @param {Phaser.Game} aGame A reference to the currently running game.
 * @param {Number} aX The x coordinate (in world space) to position the Sprite at.
 * @param {Number} aY The y coordinate (in world space) to position the Sprite at.
 * @param {any} aKey This is the image or texture used by the Sprite during rendering. It can be a string which is a reference to the Cache entry, or an instance of a RenderTexture or PIXI.Texture.
 * @param {any} aFrame If this Sprite is using part of a sprite sheet or texture atlas you can specify the exact frame to use by giving a string or numeric index.
 */
function ComboTextEffectV1(aGame, aX, aY, aKey, aFrame) {
	
	this.beforeCreate();
	
	Phaser.Sprite.call(this, aGame, aX, aY, aKey || 'combo_effText_1', aFrame == undefined || aFrame == null? null : aFrame);
	this.anchor.setTo(0.5, 0.5);
	
	this.afterCreate();
	
}

/** @type Phaser.Sprite */
var ComboTextEffectV1_proto = Object.create(Phaser.Sprite.prototype);
ComboTextEffectV1.prototype = ComboTextEffectV1_proto;
ComboTextEffectV1.prototype.constructor = ComboTextEffectV1;

/* --- end generated code --- */
// -- user code here --

//글자 텍스트 이펙트.
ComboTextEffectV1.prototype.beforeCreate = function(){
	
};

ComboTextEffectV1.prototype.afterCreate = function(){
	this.tweenA = null;	
};

ComboTextEffectV1.prototype.show = function() {
	this.visible = true;
};

ComboTextEffectV1.prototype.hide = function() {
	this.visible = false;
};

ComboTextEffectV1.prototype.setDestoryTween = function(){	
	if(this.tweenA !== null) {		
		this.tweenA.pendingDelete = true;
	}	
};

//트윈은 있는 동작만 추가한다.
ComboTextEffectV1.prototype.PlayTween = function(){	
	this.visible = true;
	this.alpha = 1;
	this.y = 300;
	this.setDestoryTween();
	this.tweenA = this.game.add.tween(this).to({ alpha: 0, y: 200 }, 800, "Quart.easeOut");	
	this.tweenA.start();
	this.tweenA.onComplete.add(this.endTweenPlay, this);
};

ComboTextEffectV1.prototype.endTweenPlay = function(){
	this.visible = false;
};