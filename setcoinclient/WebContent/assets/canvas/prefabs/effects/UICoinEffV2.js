
// -- user code here --

/* --- start generated code --- */

// Generated by  1.5.2 (Phaser v2.6.2)


/**
 * UICoinEffV2
 * @param {Phaser.Game} aGame A reference to the currently running game.
 * @param {Number} aX The x coordinate (in world space) to position the Sprite at.
 * @param {Number} aY The y coordinate (in world space) to position the Sprite at.
 * @param {any} aKey This is the image or texture used by the Sprite during rendering. It can be a string which is a reference to the Cache entry, or an instance of a RenderTexture or PIXI.Texture.
 * @param {any} aFrame If this Sprite is using part of a sprite sheet or texture atlas you can specify the exact frame to use by giving a string or numeric index.
 */
function UICoinEffV2(aGame, aX, aY, aKey, aFrame) {
	Phaser.Sprite.call(this, aGame, aX, aY, aKey || 'UICoinEffRe', aFrame == undefined || aFrame == null? 3 : aFrame);
	this.anchor.setTo(0.5, 0.5);
	this.animations.add('IDLE', [3], 10, true);
	this.animations.add('SHOW', [0, 1, 2], 10, false);
	
}

/** @type Phaser.Sprite */
var UICoinEffV2_proto = Object.create(Phaser.Sprite.prototype);
UICoinEffV2.prototype = UICoinEffV2_proto;
UICoinEffV2.prototype.constructor = UICoinEffV2;

/* --- end generated code --- */
// -- user code here --
