
// -- user code here --

/* --- start generated code --- */

// Generated by  1.5.2 (Phaser v2.6.2)


/**
 * TalkBoxCombo2V2
 * @param {Phaser.Game} aGame A reference to the currently running game.
 * @param {Number} aX The x coordinate (in world space) to position the Sprite at.
 * @param {Number} aY The y coordinate (in world space) to position the Sprite at.
 * @param {any} aKey This is the image or texture used by the Sprite during rendering. It can be a string which is a reference to the Cache entry, or an instance of a RenderTexture or PIXI.Texture.
 * @param {any} aFrame If this Sprite is using part of a sprite sheet or texture atlas you can specify the exact frame to use by giving a string or numeric index.
 */
function TalkBoxCombo2V2(aGame, aX, aY, aKey, aFrame) {
	Phaser.Sprite.call(this, aGame, aX, aY, aKey || 'sheet_talkBoxCombo22', aFrame == undefined || aFrame == null? 7 : aFrame);
	this.anchor.setTo(0.5, 1.0);
	this.animations.add('SHOW', [0, 1, 2, 8, 3, 7, 4, 6, 5, 6], 20, false);
	this.animations.add('NOTICE', [9, 10, 9, 11], 10, true);
	
}

/** @type Phaser.Sprite */
var TalkBoxCombo2V2_proto = Object.create(Phaser.Sprite.prototype);
TalkBoxCombo2V2.prototype = TalkBoxCombo2V2_proto;
TalkBoxCombo2V2.prototype.constructor = TalkBoxCombo2V2;

/* --- end generated code --- */
// -- user code here --
TalkBoxCombo2V2.prototype.setPlay = function(aAnimaiton, aIsTween) {
	var isTween = false;
	
	if(this === undefined || this.animations === undefined) { console.log("TalkBoxCombo2V2 SetPlay undefined!!!"); return;}
	if(aIsTween !== undefined && aIsTween !== null) { isTween = aIsTween; }	
	this.visible = true;	
	switch(aAnimaiton) {
		case  "SHOW" :
			this.animations.play("SHOW");
			break;
		case  "NOTICE" :
			this.animations.play("NOTICE");
			break;
	}
};

TalkBoxCombo2V2.prototype.show = function(){
	this.visible = true;
};

TalkBoxCombo2V2.prototype.hide = function(){
	this.animations.stop(null, true);
	this.visible = false;
};