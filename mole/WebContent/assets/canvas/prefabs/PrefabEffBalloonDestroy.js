
// -- user code here --

/* --- start generated code --- */

// Generated by  1.5.2 (Phaser v2.6.2)


/**
 * PrefabEffBalloonDestroy
 * @param {Phaser.Game} aGame A reference to the currently running game.
 * @param {Number} aX The x coordinate (in world space) to position the Sprite at.
 * @param {Number} aY The y coordinate (in world space) to position the Sprite at.
 * @param {any} aKey This is the image or texture used by the Sprite during rendering. It can be a string which is a reference to the Cache entry, or an instance of a RenderTexture or PIXI.Texture.
 * @param {any} aFrame If this Sprite is using part of a sprite sheet or texture atlas you can specify the exact frame to use by giving a string or numeric index.
 */
function PrefabEffBalloonDestroy(aGame, aX, aY, aKey, aFrame) {
	Phaser.Sprite.call(this, aGame, aX, aY, aKey || 'BalloonDestroySheet', aFrame == undefined || aFrame == null? 8 : aFrame);
	this.anchor.setTo(0.51, 0.51);
	this._PLAY1 = this.animations.add('PLAY1', [0, 1, 2, 3], 12, false);
	this._PLAY1.onComplete.add(this.play2, this);
	
	this._PLAY2 = this.animations.add('PLAY2', [4, 5, 9, 6, 8, 7, 8], 12, false);
	this._PLAY2.onComplete.add(this.gameOver, this);
}

/** @type Phaser.Sprite */
var PrefabEffBalloonDestroy_proto = Object.create(Phaser.Sprite.prototype);
PrefabEffBalloonDestroy.prototype = PrefabEffBalloonDestroy_proto;
PrefabEffBalloonDestroy.prototype.constructor = PrefabEffBalloonDestroy;

/* --- end generated code --- */
// -- user code here --

PrefabEffBalloonDestroy.prototype.play = function() {
	this._PLAY1.play();
	
	if (gLife > 0) {
		gLife -= 1;
		gLifeText.setText(gLife.toString());
	}
	// 꼼수:성공 실패 결과 화면에서 오답 처리되면서 중복 되는 버그 수정 
	if (gLife == 0) {	
		gGameState = -1;
	}
		
};

PrefabEffBalloonDestroy.prototype.play2 = function() {
	gFlagWrong = false;
	
	this._PLAY1.stop();
	this._PLAY2.play();
}

PrefabEffBalloonDestroy.prototype.gameOver = function() {
	if (gLife == 0) {	
		gGameState = GAME_STATE.RESULT;
	}
};

