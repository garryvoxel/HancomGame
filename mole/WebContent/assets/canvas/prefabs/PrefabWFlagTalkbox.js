
// -- user code here --

/* --- start generated code --- */

// Generated by  1.5.2 (Phaser v2.6.2)


/**
 * PrefabWFlagTalkbox
 * @param {Phaser.Game} aGame A reference to the currently running game.
 * @param {Number} aX The x coordinate (in world space) to position the Sprite at.
 * @param {Number} aY The y coordinate (in world space) to position the Sprite at.
 * @param {any} aKey This is the image or texture used by the Sprite during rendering. It can be a string which is a reference to the Cache entry, or an instance of a RenderTexture or PIXI.Texture.
 * @param {any} aFrame If this Sprite is using part of a sprite sheet or texture atlas you can specify the exact frame to use by giving a string or numeric index.
 */
function PrefabWFlagTalkbox(aGame, aX, aY, aKey, aFrame) {
	Phaser.Sprite.call(this, aGame, aX, aY, aKey || 'WFlagTalkbox', aFrame == undefined || aFrame == null? 7 : aFrame);
	this.anchor.setTo(0.5, 0.5);
	this._PLAY = this.animations.add('PLAY', [0, 1, 2, 3, 4, 5, 6, 7], 20, false);
	
	this._PLAY.onComplete.add(this.delayFadeOut, this);
	
	this.visible = false;
}

/** @type Phaser.Sprite */
var PrefabWFlagTalkbox_proto = Object.create(Phaser.Sprite.prototype);
PrefabWFlagTalkbox.prototype = PrefabWFlagTalkbox_proto;
PrefabWFlagTalkbox.prototype.constructor = PrefabWFlagTalkbox;

/* --- end generated code --- */
// -- user code here --

PrefabWFlagTalkbox.prototype.play = function(index) {
	this.index = index
	this.visible = true;
	this._PLAY.play();
};

PrefabWFlagTalkbox.prototype.delayFadeOut = function() {
	this.game.time.events.repeat(300, 1, this.fadeOut, this);
};

PrefabWFlagTalkbox.prototype.fadeOut = function() {
	this.visible = false;
	this._PLAY.stop();

	var charType = hole[this.index];
	if (charType > 0) {
		gCharLayer[charType-1].children[this.index].hitDown();
	}

};