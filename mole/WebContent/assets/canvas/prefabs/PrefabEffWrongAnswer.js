
// -- user code here --

/* --- start generated code --- */

// Generated by  1.5.2 (Phaser v2.6.2)


/**
 * PrefabEffWrongAnswer
 * @param {Phaser.Game} aGame A reference to the currently running game.
 * @param {Number} aX The x coordinate (in world space) to position the Sprite at.
 * @param {Number} aY The y coordinate (in world space) to position the Sprite at.
 * @param {any} aKey This is the image or texture used by the Sprite during rendering. It can be a string which is a reference to the Cache entry, or an instance of a RenderTexture or PIXI.Texture.
 * @param {any} aFrame If this Sprite is using part of a sprite sheet or texture atlas you can specify the exact frame to use by giving a string or numeric index.
 */
function PrefabEffWrongAnswer(aGame, aX, aY, aKey, aFrame) {
	Phaser.Sprite.call(this, aGame, aX, aY, aKey || 'WrongActionSheet', aFrame == undefined || aFrame == null? 0 : aFrame);
	this._PLAY = this.animations.add('PLAY', [0, 1, 0, 1, 0, 1], 16, false);
	this._PLAY.onComplete.add(this.hide, this);
	
	this.visible = false;
}

/** @type Phaser.Sprite */
var PrefabEffWrongAnswer_proto = Object.create(Phaser.Sprite.prototype);
PrefabEffWrongAnswer.prototype = PrefabEffWrongAnswer_proto;
PrefabEffWrongAnswer.prototype.constructor = PrefabEffWrongAnswer;

/* --- end generated code --- */
// -- user code here --


PrefabEffWrongAnswer.prototype.play = function() {
	gFlagWrong = true;
	
	this.visible = true;	
	this._PLAY.play();
};

PrefabEffWrongAnswer.prototype.hide = function() {
	this._PLAY.stop();	
	this.visible = false;
	
	gBalloonDestroySheet.play();
};