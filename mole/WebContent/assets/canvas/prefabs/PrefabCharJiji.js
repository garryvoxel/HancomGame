
// -- user code here --

/* --- start generated code --- */

// Generated by  1.5.2 (Phaser v2.6.2)


/**
 * PrefabCharJiji
 * @param {Phaser.Game} aGame A reference to the currently running game.
 * @param {Number} aX The x coordinate (in world space) to position the Sprite at.
 * @param {Number} aY The y coordinate (in world space) to position the Sprite at.
 * @param {any} aKey This is the image or texture used by the Sprite during rendering. It can be a string which is a reference to the Cache entry, or an instance of a RenderTexture or PIXI.Texture.
 * @param {any} aFrame If this Sprite is using part of a sprite sheet or texture atlas you can specify the exact frame to use by giving a string or numeric index.
 */
function PrefabCharJiji(aGame, aX, aY, aKey, aFrame) {
	Phaser.Sprite.call(this, aGame, aX, aY, aKey || 'CharJijiSheet', aFrame == undefined || aFrame == null? 0 : aFrame);
	
	this._UP = this.animations.add('UP', [5, 6, 7, 8, 9, 10, 0], 10, false);
	this._UP.onComplete.add(this.animComplete, this);
	
	this._IDLE = this.animations.add('IDLE', [0, 1, 1, 2, 3, 4, 4, 2], 10, true);
	
	this._DOWN = this.animations.add('DOWN', [0, 11, 12, 13, 13, 14, 15, 16, 17], 10, false);
	this._DOWN.onComplete.add(this.animHideComplete, this);
	
	this._HIT = this.animations.add('HIT', [20], 3, true);
	
	this._HIT_DOWN = this.animations.add('HIT_DOWN', [20, 21, 22], 10, false);
	this._HIT_DOWN.onComplete.add(this.fadeOut, this);
	this._HIT_FEAR = this.animations.add('HIT_FEAR', [18, 19], 5, true);
	
	this.anchor.setTo(0.47, 0.77);
	this.visible = false;
	
	this.charWord = "";
	
	// 190313 add
	this.isIdle = false;	// 캐릭터 idle상태 체크
	
	this.that = aGame;

}

/** @type Phaser.Sprite */
var PrefabCharJiji_proto = Object.create(Phaser.Sprite.prototype);
PrefabCharJiji.prototype = PrefabCharJiji_proto;
PrefabCharJiji.prototype.constructor = PrefabCharJiji;

/* --- end generated code --- */
// -- user code here --

PrefabCharJiji.prototype.charUp = function(_idx, _showTime) {
	if (mEffectMute == false) {
		esIn.play();
	}
	
	this.visible = true;
	this._UP.play();
	
	this.idx = _idx;

	// 190313 add
	// 상태 변경 추가
	this.isIdle = false;
	
	// 등장해서 존재할 시간 셋팅
	var _rnd;
	if (_showTime.min != _showTime.max) {
		_rnd = this.game.rnd.integerInRange(_showTime.min, _showTime.max);
	}
	else {
		_rnd = _showTime.max;
	}
	
	// 몇초 후 사라질지 이벤트 등록
	this.charEvent = this.game.time.events.repeat(_rnd*1000, 1, this.hide, this);
	
};

PrefabCharJiji.prototype.hitFear = function() {
	this.visible = true;
	this._IDLE.stop();
	this._HIT_FEAR.play();
	
	// 190313 add
	// 상태 변경 추가
//	this.isIdle = false;
	
	this.game.state.getCurrentState().wordPanel.children[this.idx].hide();
};

PrefabCharJiji.prototype.hit = function() {
	this.visible = true;
	this._HIT_FEAR.stop();
	this._HIT.play();
};

PrefabCharJiji.prototype.hitDown = function() {
	this.visible = true;
	this._HIT.stop();
	this._HIT_DOWN.play();
};

PrefabCharJiji.prototype.fadeOut = function() {
	hole[this.idx] = 0;
	this.visible = false;
	
	this.isIdle = false;
};

PrefabCharJiji.prototype.showWordBox = function() {
	this.game.state.getCurrentState().wordPanel.children[this.idx].show(this.charWord);
	
	this._UP.stop();
	this._IDLE.play();
	
	// 190313 add
	// 상태 변경 추가
	this.isIdle = true;
};

PrefabCharJiji.prototype.animComplete = function() {
	this.showWordBox();
};

PrefabCharJiji.prototype.hide = function() {
	if (mEffectMute == false) {
		esOut.play();
	}
	
	if(itemSpawnTable[this.idx] != 0) {
		gCorrectIndex = this.idx;
		gState = 3; 
	}
	
	this.game.state.getCurrentState().wordPanel.children[this.idx].hide();
	this._IDLE.stop();
	this._DOWN.play();
	
	// 190313 add
	// 상태 변경 추가
	this.isIdle = false;
	
	// 190323 add
	correctWordList[this.idx] = "";
};

PrefabCharJiji.prototype.animHideComplete = function() {
	this.game.time.events.remove(this.charEvent);
	
	hole[this.idx] = 0;
	this.visible = false;
};

PrefabCharJiji.prototype.cancelEvent = function() {
	if (this._IDLE.isPlaying) {
		this._IDLE.stop();
		
		// 190313 add
		// 상태 변경 추가
//		this.isIdle = false;
	}
	
	this.game.time.events.remove(this.charEvent);	// ok	
	this._HIT_DOWN.play();
};

PrefabCharJiji.prototype.setCharInfo = function(_word) {
	this.charWord = _word;
};
