
// -- user code here --

/* --- start generated code --- */

// Generated by  1.5.3 (Phaser v2.6.2)


/**
 * itemIce
 * @param {Phaser.Game} aGame A reference to the currently running game.
 * @param {Number} aX The x coordinate (in world space) to position the Sprite at.
 * @param {Number} aY The y coordinate (in world space) to position the Sprite at.
 * @param {any} aKey This is the image or texture used by the Sprite during rendering. It can be a string which is a reference to the Cache entry, or an instance of a RenderTexture or PIXI.Texture.
 * @param {any} aFrame If this Sprite is using part of a sprite sheet or texture atlas you can specify the exact frame to use by giving a string or numeric index.
 */
function itemIce(aGame, aX, aY, aKey, aFrame) {
	Phaser.Sprite.call(this, aGame, aX, aY, aKey || 'ItemIcePanel', aFrame == undefined || aFrame == null? 0 : aFrame);
	this.alpha = 0.5;
	this.anchor.setTo(0.5, 0.0);
	var _anim_ice = this.animations.add('ice', [0, 1, 2, 3, 4, 5, 6, 7, 8], 6, true);
	_anim_ice.play();
	
	this.afterCreate();
	
}

/** @type Phaser.Sprite */
var itemIce_proto = Object.create(Phaser.Sprite.prototype);
itemIce.prototype = itemIce_proto;
itemIce.prototype.constructor = itemIce;

/* --- end generated code --- */
// -- user code here --

itemIce.prototype.afterCreate = function() {
	
	this.iceSeconds = 3.5;
	
	//input disable
	this.game.gameManager.inputTextUnavailable = true;
	
	disappearDelay = (Phaser.Timer.SECOND * this.iceSeconds) - 400;
	
	appear = this.game.add.tween(this).to({alpha : 1}, 200, "Linear", true);
	disappear = this.game.add.tween(this).to({alpha : 0}, 200, "Linear", false, disappearDelay);
	
	appear.chain(disappear);
	disappear.onComplete.add(this.killAndUnlock, this);
	
	//this.game.time.events.add(Phaser.Timer.SECOND * this.iceSeconds, this.killAndUnlock, this);
	
	this.game.audioManager.playSfx('Card_it5');
};

itemIce.prototype.killAndUnlock = function() {
	
	console.log("kill ice");
	
	//input enable
	this.game.gameManager.inputTextUnavailable = false;
	this.destroy();
};